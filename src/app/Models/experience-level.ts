
  export interface ExperienceLevel {
    id: number;                    // Unique ID for the experience level
    level: string;                 // Name of the experience level (e.g., Fresher, Mid, Senior)
  }
